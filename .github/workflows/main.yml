# https://github.com/vercel/next.js/commits/canary/.github/workflows/build_test_deploy.yml

name: CI

on:
  pull_request:
    types: [opened, synchronize]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: 'Lint: ${{ matrix.os }}'
    needs: build
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        OS: [ubuntu-latest]
    steps:
      - name: Check out repository
        uses: actions/checkout@v3

      - name: Setup PNPM
        uses: pnpm/action-setup@v2.2.1

      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: 16
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile
      - run: pnpm manypkg check
      - name: Turborepo local server
        uses: felixmosh/turborepo-gh-artifacts@v1
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          server-token: ${{ secrets.TURBO_SERVER_TOKEN }}
      - name: Build
        run: pnpm build --api="http://127.0.0.1:9080" --token="${{ secrets.TURBO_SERVER_TOKEN }}" --team="lin"
      - name: Lint
        run: pnpm lint --api="http://127.0.0.1:9080" --token="${{ secrets.TURBO_SERVER_TOKEN }}" --team="lin"
    
  build:
      runs-on: ${{ matrix.os }}
      strategy:
        matrix:
          os:
            - ubuntu-latest
          node_version:
            - 16
      name: Node ${{ matrix.node_version }} - ${{ matrix.os }}
      steps:
        - uses: actions/checkout@v2
        - uses: pnpm/action-setup@646cdf48217256a3d0b80361c5a50727664284f2
          with:
            version: 6.32.6
        - name: Setup node
          uses: actions/setup-node@v2
          with:
            node-version: ${{ matrix.node_version }}
            cache: "pnpm"
        - run: pnpm install --no-frozen-lockfile
        - run: pnpm manypkg check
        - run: pnpm build
        - run: pnpm lint
        - run: pnpm build:apps

#   build:
#     name: 'Build: ${{ matrix.os }}'
#     runs-on: ${{ matrix.os }}
#     strategy:
#       matrix:
#         OS: [ubuntu-latest]
#       fail-fast: true
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v3

#       - name: Setup PNPM
#         uses: pnpm/action-setup@v2.2.1

#       - name: Setup node@${{ matrix.NODE_VERSION }}
#         uses: actions/setup-node@v3
#         with:
#           node-version: ${{ matrix.NODE_VERSION }}
#           cache: 'pnpm'

#       - name: Install dependencies
#         run: pnpm install --no-frozen-lockfile
#       - run: pnpm manypkg check
      
#       - name: Turborepo local server
#         uses: felixmosh/turborepo-gh-artifacts@v1
#         with:
#           repo-token: ${{ secrets.GITHUB_TOKEN }}
#           server-token: ${{ secrets.TURBO_SERVER_TOKEN }}

#       - name: Build
#         run: pnpm build
#       - name: Build Apps
#         run: pnpm build:apps

  test:
    name: 'Test: ${{ matrix.os }} (node@${{ matrix.NODE_VERSION }})'
    runs-on: ${{ matrix.os }}
    needs: build
    strategy:
      matrix:
        OS: [ubuntu-latest, windows-latest]
        NODE_VERSION: [16]
      fail-fast: false
    env:
      NODE_VERSION: ${{ matrix.NODE_VERSION }}
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup PNPM
        uses: pnpm/action-setup@v2.2.1

      - name: Setup node@${{ matrix.NODE_VERSION }}
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.NODE_VERSION }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile
      - run: pnpm manypkg check

      - name: Turborepo local server
        uses: felixmosh/turborepo-gh-artifacts@v1
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          server-token: ${{ secrets.TURBO_SERVER_TOKEN }}

      - name: Build
        run: pnpm build --api="http://127.0.0.1:9080" --token="${{ secrets.TURBO_SERVER_TOKEN }}" --team="win"

      - name: Test
        run: pnpm run test --api="http://127.0.0.1:9080" --token="${{ secrets.TURBO_SERVER_TOKEN }}" --team="win"
            
