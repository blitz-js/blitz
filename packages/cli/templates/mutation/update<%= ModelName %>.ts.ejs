import db, {<%= ModelName %>UpdateArgs} from 'db'

export default async function update<%= ModelName %>(args: <%= ModelName %>UpdateArgs) {
  // Don't allow updating ID
  delete args.data.id

  const <%= modelName %> = await db.<%= modelName %>.update(args)

  return <%= modelName %>
}
