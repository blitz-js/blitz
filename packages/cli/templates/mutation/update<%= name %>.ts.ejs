import db, {<%= name %>UpdateArgs} from 'db'

export default async function update<%= name %>(args: <%= name %>UpdateArgs) {
  // Don't allow updating ID
  delete args.data.id

  const <%= lowerCaseName %> = await db.<%= lowerCaseName %>.update(args)

  return <%= lowerCaseName %>
}
