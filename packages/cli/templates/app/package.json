{
  "name": "<%= name %>",
  "version": "1.0.0",
  "scripts": {
    "build": "blitz db migrate && blitz build"
  },
  "dependencies": {
    "@blitzjs/cli": "latest",
    "@blitzjs/core": "latest",
    "@blitzjs/server": "latest",
    "react": "experimental",
    "react-dom": "experimental",
    "typescript": "latest",
    "@prisma/cli": "latest",
    "@prisma/client": "latest"
  },
  "NOTE": "Next.js dependency is currently only required for deploying to zeit",
  "devDependencies": {
    "next": "9.3.4"
  },
  "private": true
}
