{
  "name": "store",
  "private": true,
  "version": "1.0.0",
  "main": "index.js",
  "author": "BlitzJS",
  "license": "MIT",
  "scripts": {
    "start": "blitz start",
    "db:setup": "prisma2 generate && prisma2 migrate up --experimental",
    "db:migrate": "prisma2 migrate save --experimental && prisma2 migrate up --experimental",
    "build": "blitz build",
    "test": "jest --coverage",
    "test:watch": "jest --watch"
  },
  "dependencies": {
    "@blitzjs/cli": "*",
    "@blitzjs/core": "*",
    "@blitzjs/server": "*",
    "@prisma/client": "2.0.0-preview024",
    "next": "^9.3.0",
    "prisma2": "2.0.0-preview024",
    "react": "16.12.0",
    "react-dom": "16.12.0"
  },
  "devDependencies": {
    "jest": "25.1.0",
    "ts-jest": "25.2.1"
  }
}
